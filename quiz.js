//code run for quiz this week

let text = 'kittens' 
let indexOfZ = text.indexOf('z') //index is not here so -1. if k =0 or s = 6.
console.log(indexOfZ)

let trees = ['oak', 'elm', 'ash'] // trees is only three in a array here .push adds maple.
trees.push('maple')
console.log(trees)

//can I alter this or learn to use it? 
// array.forEach(element => {
    
// });